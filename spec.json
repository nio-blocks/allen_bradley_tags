{
  "namespace/BlockName": {
    "version": "1.0.0",
    "description": "Description of the block.",
    "properties": {
      "<property1>": {
        "description": "Required description of the property"
      },
      "<property2>": {
        "default": "Optional metadata with any key.",
        "description": "Required description of the property"
      }
    },
    "inputs": {
      "<input1 | default>": {
        "description": "Required description of the input"
      },
      "<input2>": {
        "description": "Required description of the input"
      }
    },
    "outputs": {
      "<output1 | default>": {
        "description": "Required description of the output"
      },
      "<output2>": {
        "description": "Required description of the output"
      }
    },
    "commands": {
      "<command1>": {
        "description": "Required description of the command",
        "type": "Optional type and other metadata with any key."
      },
      "<command2>": {
        "description": "Required description of the command"
      }
    }
  }
}
